#!javascript
//# spec/timeoutDialog-spec.js
var timeoutDialog = require("../timeoutDialog");

describe("options", function () {
  it("should show timeout", function () {
    var product = calculator.multiply(2, 3);
    expect(product).toBe(900);
  });
}); 